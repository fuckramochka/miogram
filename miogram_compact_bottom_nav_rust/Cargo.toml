[package]
name = "compact_bottom_nav"
version = "1.0.0"
edition = "2021"
authors = ["@YouRooni", "Miogram Team"]
description = "Compact bottom navigation bar without labels and with floating search button"

[lib]
crate-type = ["cdylib"]

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
panic = "abort"
strip = true
